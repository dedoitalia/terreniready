services:
  - type: web
    name: terreniready
    runtime: node
    plan: free
    region: frankfurt
    autoDeploy: true
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 22.22.0
